"""
Задание 5: Объяснение сложного кода

Выбранный фрагмент: функция расчёта прогресса тренировки
Файл: app/refactored_code.py → функция calculate_training_progress()

Бизнес-логика: расчёт общего прогресса спортсмена на основе 8 метрик.
"""

from app.refactored_code import calculate_training_progress

# =====================================================================
# ОБЪЯСНЕНИЕ РАБОТЫ КОДА (простым языком)
# =====================================================================
#
# Функция calculate_training_progress() принимает 8 числовых оценок
# (сила, выносливость, гибкость, скорость, точность, восстановление,
# регулярность, интенсивность) и превращает их в одну итоговую цифру —
# общий прогресс тренировки.
#
# Алгоритм можно разбить на 5 шагов:
#
# Шаг 1 — Взвешивание метрик
# Каждая метрика умножается на свой коэффициент важности.
# Например, сила умножается на 2.5 (самый важный показатель),
# а гибкость — всего на 0.3.
#
# Шаг 2 — Усиление высоких результатов
# Если взвешенная метрика превышает 100, она дополнительно умножается
# на 1.1 (10% бонус). Это мотивирует спортсмена достигать высоких
# показателей.
#
# Шаг 3 — Группировка
# 5 метрик разбиваются на 3 группы:
#   - Силовая группа: сила + выносливость (усредняются и * 0.7)
#   - Техническая группа: гибкость + скорость (усредняются и * 0.7)
#   - Ментальная группа: точность (* 0.3, сама по себе)
#
# Шаг 4 — Коррекция итога
# Если итог > 250 — применяется понижающий коэффициент 0.85 (чтобы
# слишком высокие результаты не уходили в бесконечность).
# Если итог < 50 — применяется повышающий коэффициент 1.2 (чтобы
# начинающие спортсмены не получали демотивирующе низкие оценки).
#
# Шаг 5 — Бонусы
# За каждую метрику, превышающую 80 баллов, начисляется 10 бонусных
# очков. Это поощряет сбалансированное развитие.
#
# =====================================================================
# ПРЕДЛОЖЕНИЯ ПО УЛУЧШЕНИЮ
# =====================================================================
#
# 1. Вынести коэффициенты в отдельный конфигурационный файл/класс,
#    чтобы их можно было менять без редактирования кода.
#
# 2. Добавить логирование промежуточных значений для отладки.
#
# 3. Сделать коэффициенты настраиваемыми через параметры функции
#    (со значениями по умолчанию), чтобы разные типы тренировок
#    могли использовать разные веса.
#
# 4. Добавить асинхронность, если функция будет использоваться
#    в эндпоинтах API.
#
# 5. Покрыть unit-тестами граничные случаи:
#    - все метрики = 0
#    - все метрики = максимальны (200)
#    - одна метрика = 0, остальные = 100
#

# Демонстрация работы функции
if __name__ == "__main__":
    # Пример: средний прогресс
    result = calculate_training_progress(
        strength_score=80.0,
        endurance_score=70.0,
        flexibility_score=60.0,
        speed_score=75.0,
        accuracy_score=85.0,
        recovery_score=65.0,
        consistency_score=90.0,
        intensity_score=70.0,
    )
    print(f"Прогресс тренировки: {result}")