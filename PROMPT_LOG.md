# Prompt Log

## Задание 1. Генерация CRUD-приложения

### Промпт 1
**Инструмент:** Cursor (Claude 3.5 Sonnet)
**Промпт:** "Ты — senior Python разработчик. Используя FastAPI, создай CRUD приложение для управления упражнениями на платформе спортивных тренировок. Модель упражнения содержит: id (int), name (str), description (str, опционально), category (str): силовая/кардио/растяжка, difficulty (str): лёгкий/средний/сложный, calories_per_hour (float, ≥0). Добавь эндпоинты: GET /exercises, POST /exercises, GET /exercises/{id}, PUT /exercises/{id}, DELETE /exercises/{id}. Используй Pydantic для валидации. Добавь обработку ошибок (404, 422). Покрой код type hints."
**Результат:** Сгенерированы файлы main.py, schemas.py, crud.py, database.py, models.py. Пришлось вручную поправить ExerciseResponse — устаревший class Config заменить на model_config для Pydantic v2.

---

## Задание 2. Генерация тестов

### Промпт 2
**Инструмент:** Cursor (Claude 3.5 Sonnet)
**Промпт:** "Напиши модульные тесты (pytest) для FastAPI CRUD приложения управления упражнениями. Используй in-memory SQLite для изоляции тестов. Тесты должны покрывать: создание упражнения (включая валидацию полей: неверная сложность, отрицательные калории, пустое имя), получение списка (пустая БД, с данными, фильтр по категории, пагинация), получение по ID (успех и 404), обновление (успех, 404, невалидные данные), удаление (успех и 404). Используй TestClient из FastAPI."
**Результат:** 15 тестов, сгруппированных по классам (TestCreateExercise, TestReadExercises, TestReadExercise, TestUpdateExercise, TestDeleteExercise). Все тесты проходят успешно.

---

## Задание 3. Рефакторинг «плохого» кода

### Промпт 3
**Инструмент:** Cursor (Claude 3.5 Sonnet)
**Промпт:** "Напиши плохо написанную функцию на Python для расчёта прогресса тренировки. Функция должна принимать 8 параметров с бессмысленными именами (a, b, c...). Используй магические числа, дублирование кода (5 одинаковых if-блоков), длину >30 строк, отсутствие обработки ошибок, неинформативные имена переменных (x, y, z, w, v, r1, r2, r3)."
**Результат:** Создан файл bad_code.py с функцией calc(), содержащей все указанные проблемы.

### Промпт 4
**Инструмент:** Cursor (Claude 3.5 Sonnet)
**Промпт:** "Выполни рефакторинг этой плохо написанной функции. Исправь: 1) замени магические числа на именованные константы; 2) дай переменным осмысленные имена; 3) убери дублирование кода через list comprehension; 4) добавь type hints; 5) добавь docstring с описанием параметров и возвращаемого значения; 6) добавь валидацию входных данных с ValueError; 7) объясни каждое изменение в комментариях."
**Результат:** Создан файл refactored_code.py с функцией calculate_training_progress(). Все проблемы исправлены, добавлены константы, type hints, docstring, валидация.

---

## Задание 4. Генерация Docker-конфигурации

### Промпт 5
**Инструмент:** Cursor (Claude 3.5 Sonnet)
**Промпт:** "Создай Dockerfile для FastAPI-приложения. Используй python:3.11-slim, установи зависимости из requirements.txt, скопируй код из папки app, открой порт 8000, запусти через uvicorn app.main:app."
**Результат:** Dockerfile создан, корректно собирает образ.

### Промпт 6
**Инструмент:** Cursor (Claude 3.5 Sonnet)
**Промпт:** "Создай docker-compose.yml для FastAPI-приложения. Добавь сервис app, который собирается из текущей директории, пробрасывает порт 8000, использует переменную окружения DATABASE_URL. Добавь закомментированный блок для PostgreSQL (образ postgres:16-alpine, пользователь fitness, БД fitness_platform)."
**Результат:** docker-compose.yml создан, при запуске через Docker приложение работает.

---

## Задание 5. Объяснение сложного кода

### Промпт 7
**Инструмент:** Cursor (Claude 3.5 Sonnet)
**Промпт:** "Объясни простым языком, как работает функция calculate_training_progress() из файла refactored_code.py. Алгоритм включает взвешивание 8 метрик, усиление высоких результатов, группировку в 3 категории (силовая, техническая, ментальная), коррекцию итога и бонусы. Разбей объяснение на 5 шагов. Также предложи 5 улучшений для этого кода."
**Результат:** Создан файл complex_code_explanation.py с подробным объяснением на русском языке и 5 предложениями по улучшению.

---

## Задание 6. Генерация документации

### Промпт 8
**Инструмент:** Cursor (Claude 3.5 Sonnet)
**Промпт:** "Создай README.md на русском языке для проекта Fitness Training Platform API. Документация должна включать: описание проекта, инструкцию по установке и запуску (локально и через Docker), описание всех API-эндпоинтов с примерами запросов curl и JSON-ответов (включая 200, 201, 204 и 404), описание переменных окружения, инструкцию по запуску тестов, структуру проекта."
**Результат:** Создан README.md на русском языке со всеми разделами, примерами запросов и ответов.

---

## Задание 7. Генерация миграций БД

### Промпт 9
**Инструмент:** Cursor (Claude 3.5 Sonnet)
**Промпт:** "Настрой Alembic для FastAPI-приложения. Нужно: инициализировать Alembic, настроить подключение к SQLite, настроить env.py для автогенерации (импорт Base.metadata из app.database). Модели уже есть — добавь к ним связи: Exercise (многие-ко-многим с Workout через workout_exercises, один-ко-многим с Progress), Workout (связь многие-ко-многим с Exercise), Progress (связь с Exercise). Сгенерируй автоматическую миграцию."
**Результат:** Alembic настроен, модели расширены связями, сгенерирована и применена миграция (созданы таблицы workouts, workout_exercises, progress с индексами и внешними ключами).

---

## Задание 8. Поиск уязвимостей в коде

### Промпт 10
**Инструмент:** Cursor (Claude 3.5 Sonnet)
**Промпт:** "Ты — security code reviewer. Проведи анализ безопасности FastAPI-приложения для управления упражнениями. Проверь на: 1) SQL-инъекции, 2) XSS, 3) отсутствие аутентификации, 4) path traversal, 5) незакрытые соединения с БД, 6) rate limiting, 7) утечку информации через ошибки, 8) IDOR. Для каждой найденной уязвимости напиши статус, описание и рекомендацию по исправлению."
**Результат:** Составлен отчёт docs/vulnerability_report.md с анализом 8 типов уязвимостей. Найдены проблемы: отсутствие аутентификации, rate limiting, утечка деталей ошибок, IDOR. Добавлены примеры кода для исправления.

---

## Задание 9. Генерация SQL-запросов

### Промпт 11
**Инструмент:** Cursor (Claude 3.5 Sonnet)
**Промпт:** "На основе модели данных платформы спортивных тренировок (таблицы: exercises, workouts, workout_exercises, progress) напиши SQL-запрос, который показывает топ-10 самых популярных упражнений по частоте включения в тренировки за последние 30 дней. В результат включи: ID упражнения, название, категорию, количество включений, средний балл прогресса. Объясни логику каждого JOIN, WHERE, GROUP BY и ORDER BY. Предоставь версии для PostgreSQL и SQLite."
**Результат:** Создан файл sql_query_report.py с SQL-запросами для PostgreSQL и SQLite, подробным построчным объяснением логики.

---

## Задание 10. Генерация регулярного выражения

### Промпт 12
**Инструмент:** Cursor (Claude 3.5 Sonnet)
**Промпт:** "Сгенерируй регулярное выражение для валидации ID тренировки на платформе спортивных тренировок. Формат: 3 заглавные латинские буквы + дефис + 4 цифры (например, WRK-0001). Также напиши Python-скрипт с функцией is_valid_workout_id() и тестированием на 5 валидных и 10 невалидных примерах (строчные буквы, неправильное количество символов, неверный разделитель, пробелы, пустая строка, цифры вместо букв)."
**Результат:** Создан файл regex_validation.py с регулярным выражением ^[A-Z]{3}-\d{4}$, функцией валидации и тестами на 15 примерах. Все тесты пройдены.

---

### Итого
- **Количество промптов:** 12
- **Что пришлось исправлять вручную:** замена устаревшего class Config на model_config в Pydantic схеме (ExerciseResponse), удаление неиспользуемых импортов (List в refactored_code.py, Tuple в regex_validation.py), замена datetime.utcnow() на datetime.now(timezone.utc) в models.py
- **Время:** ~60 мин
- **Инструменты:** Cursor (Claude 3.5 Sonnet)